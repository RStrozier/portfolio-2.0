export interface StorySlides {
    text: string;
    image: string;
    order: number;
  }

  export interface Project {
    name: string;
    year: number;
    language: string;
    description: string;
    url: string;
  }
